// Interfaces
import { NavigatorState } from '../interfaces/navigator-state';

/**
 * Core initial state
 */
export const navigatorInitialState: NavigatorState = {
    data: null,
    ui: {
        isVisible: true
    }
};
